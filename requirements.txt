python-dotenv==1.0.0
django==5.2.7
requests==2.31.0

# To install packages in the future, just add them to requirements.txt and run:
# source .venv/bin/activate
# pip install -r requirements.txt
#
# Or to save currently installed packages:
# pip freeze > requirements.txt